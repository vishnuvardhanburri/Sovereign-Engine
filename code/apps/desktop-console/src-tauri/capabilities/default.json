{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop console permissions. Queueing and sending APIs are intentionally not exposed to IPC.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
